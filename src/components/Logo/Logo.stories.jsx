import React from "react";
import { Logo } from "./Logo";

export default {
  title: "Components/Logo",
  component: Logo,
};

const Template = () => <Logo/>

export const Default = Template.bind({});
